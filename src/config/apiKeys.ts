/**
 * API keys configuration
 *
 * IMPORTANT: API keys have been moved to the server-side for security.
 * This file is kept for backward compatibility but no longer contains actual keys.
 *
 * @deprecated Use apiConfig.ts instead
 */

// These are now empty as the keys are stored securely on the server
export const OCR_API_KEY = "";
export const HUGGINGFACE_API_TOKEN = "";
